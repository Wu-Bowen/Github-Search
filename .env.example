# Optional: GitHub Personal Access Token for higher rate limits
#
# Rate Limits:
# - Search API (unauthenticated): 10 requests/minute
# - Search API (authenticated): 30 requests/minute
# - Core API (unauthenticated): 60 requests/hour
# - Core API (authenticated): 5,000 requests/hour
#
# Create token at: https://github.com/settings/tokens
# Required scopes: None (just leave all checkboxes unchecked for public data)
VITE_GITHUB_TOKEN=

# Optional: Override GitHub API base URL (default: https://api.github.com)
VITE_GITHUB_API_BASE=https://api.github.com
